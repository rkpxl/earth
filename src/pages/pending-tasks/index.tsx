import PendingTask from '../../Scenes/PendingTasks';
import Layout from '../../Scenes/Home'

const PendingTasks = () => {
  return (
    <Layout><PendingTask /></Layout>
  );
};

export default PendingTasks;
